[package]
name = "crfsuite-cli"
version = "0.1.0"
edition = "2021"
description = "Rust CLI for CRFsuite"

[[bin]]
name = "crfsuite-rs"
path = "src/main.rs"

[dependencies]
crfsuite-core = { path = "../crfsuite-core" }
clap = { version = "4", features = ["derive"] }
